name: 'print-message'
description: 'Prints a provided message'
inputs:
  message:
    description: 'The message to print'
    required: true
runs:
  using: 'composite'
  steps:
    - name: Print the message
      run: echo "${{ inputs.message }}"
      shell: bash
